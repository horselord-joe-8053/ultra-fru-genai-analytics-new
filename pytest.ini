[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    integration: tests that need a running stack (skipped by default in CI unit job)
    embedding_profile: optional ModelArk / skylark_2048 embedding lane tests
filterwarnings =
    ignore::DeprecationWarning
