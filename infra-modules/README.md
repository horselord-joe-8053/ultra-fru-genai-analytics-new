
# infra-modules

Reusable OpenTofu/Terraform modules.

- `primitives/`: foundational building blocks used by all clouds/scopes
- `aws/`: AWS realizations
- `gcp/`: GCP realizations

Modules are environment-agnostic; env/region live in deploy projects.
