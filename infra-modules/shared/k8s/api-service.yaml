apiVersion: v1
kind: Service
metadata:
  name: fru-api-svc
  namespace: fru-kube
spec:
  selector:
    app: fru-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
  type: LoadBalancer
