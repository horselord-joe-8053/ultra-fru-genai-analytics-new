
"""
Legacy helper (kept for reference).

Spark scheduling on ECS is now managed by Terraform in `live-deploy-aws/nonkube`.
This script can be used for ad-hoc `run-task` experiments if needed.
"""
print("Spark ECS scheduling is Terraform-managed. Use tools/aws/deploy.py for bootstrap.")
